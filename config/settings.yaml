config:
  libraries:
    gcp:
      params:
        destination: gcp
        source: github.com/aroq/toolbox-gcp
        image: aroq/toolbox-gcp
      commands:
        gcloud:
          params:
            cmd: gcp/gcloud
tools:
  gcloud:
    from: config.libraries.gcp.commands.gcloud
